module.exports = [
"[externals]/assert [external] (assert, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("assert", () => require("assert"));

module.exports = mod;
}),
"[externals]/buffer [external] (buffer, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("buffer", () => require("buffer"));

module.exports = mod;
}),
"[externals]/crypto [external] (crypto, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("crypto", () => require("crypto"));

module.exports = mod;
}),
"[externals]/events [external] (events, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("events", () => require("events"));

module.exports = mod;
}),
"[externals]/http [external] (http, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("http", () => require("http"));

module.exports = mod;
}),
"[externals]/https [external] (https, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("https", () => require("https"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/action-async-storage.external.js [external] (next/dist/server/app-render/action-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/action-async-storage.external.js", () => require("next/dist/server/app-render/action-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/runtime-reacts.external.js [external] (next/dist/server/runtime-reacts.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/runtime-reacts.external.js", () => require("next/dist/server/runtime-reacts.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/node:stream [external] (node:stream, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("node:stream", () => require("node:stream"));

module.exports = mod;
}),
"[externals]/querystring [external] (querystring, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("querystring", () => require("querystring"));

module.exports = mod;
}),
"[externals]/url [external] (url, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("url", () => require("url"));

module.exports = mod;
}),
"[externals]/util [external] (util, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("util", () => require("util"));

module.exports = mod;
}),
"[externals]/zlib [external] (zlib, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("zlib", () => require("zlib"));

module.exports = mod;
}),
"[project]/app/api/portal/data/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "GET",
    ()=>GET
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/auth.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$db$2f$portal$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/db/portal.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2d$auth$2f$next$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next-auth/next/index.js [app-route] (ecmascript)");
;
;
;
;
async function GET() {
    const session = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2d$auth$2f$next$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getServerSession"])(__TURBOPACK__imported__module__$5b$project$5d2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["authOptions"]);
    if (!session?.user || session.user.role === "super_admin") {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "Not authorized."
        }, {
            status: 403
        });
    }
    try {
        const data = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$db$2f$portal$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getPortalData"])({
            role: session.user.role,
            orgOwnerId: session.user.orgOwnerId,
            clientId: session.user.clientId,
            employeeId: session.user.employeeId
        });
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json(data);
    } catch (err) {
        console.error("Failed to load portal data — is MONGODB_URI configured?", err);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            error: "Couldn't reach the database."
        }, {
            status: 503
        });
    }
}
}),
"[project]/auth.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "auth",
    ()=>auth,
    "authOptions",
    ()=>authOptions,
    "default",
    ()=>__TURBOPACK__default__export__
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2d$auth$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next-auth/index.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2d$auth$2f$providers$2f$credentials$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next-auth/providers/credentials.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$bcryptjs$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/bcryptjs/index.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$db$2f$users$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/db/users.ts [app-route] (ecmascript)");
;
;
;
;
const authOptions = {
    session: {
        strategy: "jwt"
    },
    pages: {
        signIn: "/login"
    },
    providers: [
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2d$auth$2f$providers$2f$credentials$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"])({
            name: "Email and password",
            credentials: {
                email: {
                    label: "Email",
                    type: "email"
                },
                password: {
                    label: "Password",
                    type: "password"
                }
            },
            async authorize (credentials) {
                const email = credentials?.email;
                const password = credentials?.password;
                if (typeof email !== "string" || typeof password !== "string") {
                    return null;
                }
                let user;
                try {
                    user = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$db$2f$users$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["findUserByEmail"])(email);
                } catch (err) {
                    console.error("Auth lookup failed — is MONGODB_URI configured?", err);
                    throw new Error("Couldn't reach the database. Please try again shortly.");
                }
                if (!user) {
                    return null;
                }
                if (!user.passwordHash) {
                    return null;
                }
                const valid = await __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$bcryptjs$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"].compare(password, user.passwordHash);
                if (!valid) {
                    return null;
                }
                return {
                    id: user._id,
                    name: user.name,
                    email: user.email,
                    role: user.role,
                    orgOwnerId: user.orgOwnerId,
                    clientId: user.clientId,
                    employeeId: user.employeeId
                };
            }
        })
    ],
    callbacks: {
        async jwt ({ token, user }) {
            if (user) {
                token.id = user.id;
                token.role = user.role;
                token.orgOwnerId = user.orgOwnerId;
                token.clientId = user.clientId;
                token.employeeId = user.employeeId;
            }
            return token;
        },
        async session ({ session, token }) {
            if (session.user && token.id) {
                session.user.id = token.id;
                session.user.role = token.role;
                session.user.orgOwnerId = token.orgOwnerId;
                session.user.clientId = token.clientId ?? null;
                session.user.employeeId = token.employeeId ?? null;
            }
            return session;
        }
    }
};
const auth = ()=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2d$auth$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getServerSession"])(authOptions);
const __TURBOPACK__default__export__ = auth;
}),
"[project]/lib/aggregate.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// lib/aggregate.ts
__turbopack_context__.s([
    "grossForPeriod",
    ()=>grossForPeriod,
    "lifetimeCollected",
    ()=>lifetimeCollected,
    "paymentFor",
    ()=>paymentFor,
    "paymentsForStudent",
    ()=>paymentsForStudent,
    "payrollByDepartment",
    ()=>payrollByDepartment,
    "projectedGross",
    ()=>projectedGross,
    "projectedNet",
    ()=>projectedNet,
    "schoolFinancials",
    ()=>schoolFinancials,
    "supplyStock",
    ()=>supplyStock
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$payroll$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/payroll.ts [app-route] (ecmascript)");
;
function projectedGross(employees, fields) {
    return employees.filter((e)=>e.status !== "inactive").reduce((sum, e)=>sum + (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$payroll$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["computePayslip"])(e, fields).gross, 0);
}
function projectedNet(employees, fields) {
    return employees.filter((e)=>e.status !== "inactive").reduce((sum, e)=>sum + (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$payroll$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["computePayslip"])(e, fields).net, 0);
}
function payrollByDepartment(employees, fields, departments) {
    return departments.map((department)=>{
        const staff = employees.filter((e)=>e.departmentId === department.id && e.status !== "inactive");
        const amount = staff.reduce((sum, e)=>sum + (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$payroll$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["computePayslip"])(e, fields).gross, 0);
        return {
            department,
            amount,
            headcount: staff.length
        };
    }).sort((a, b)=>b.amount - a.amount);
}
function paymentFor(payments, studentId, period) {
    return payments.find((p)=>p.studentId === studentId && p.period === period);
}
function paymentsForStudent(payments, studentId) {
    return payments.filter((p)=>p.studentId === studentId).sort((a, b)=>a.period < b.period ? 1 : -1);
}
function lifetimeCollected(payments, studentId) {
    return payments.filter((p)=>p.studentId === studentId).reduce((sum, p)=>sum + p.amountPaid, 0);
}
function schoolFinancials(students, payments, expenses, period) {
    let feesCollected = 0;
    let feesOutstanding = 0;
    let unpaidCount = 0;
    let socialCaseCount = 0;
    for (const student of students){
        const record = paymentFor(payments, student.id, period);
        const paid = record?.amountPaid ?? 0;
        feesCollected += paid;
        if (record?.status === "social_case") {
            socialCaseCount += 1;
        } else {
            feesOutstanding += Math.max(0, student.monthlyFee - paid);
            if (!record || record.status === "unpaid") unpaidCount += 1;
        }
    }
    const expensesThisMonth = expenses.filter((e)=>e.date.slice(0, 7) === period).reduce((sum, e)=>sum + e.amount, 0);
    return {
        studentCount: students.length,
        feesCollected,
        feesOutstanding,
        unpaidCount,
        socialCaseCount,
        expensesThisMonth
    };
}
function grossForPeriod(period, payslips, employees, fields) {
    const generated = payslips.filter((p)=>p.period === period);
    if (generated.length > 0) {
        return generated.reduce((sum, p)=>sum + p.gross, 0);
    }
    return projectedGross(employees, fields);
}
function supplyStock(deliveries, sales) {
    const rows = new Map();
    function rowFor(category, itemLabel) {
        const key = `${category}::${itemLabel}`;
        let row = rows.get(key);
        if (!row) {
            row = {
                category,
                itemLabel,
                delivered: 0,
                sold: 0,
                stock: 0,
                revenue: 0
            };
            rows.set(key, row);
        }
        return row;
    }
    for (const d of deliveries){
        const row = rowFor(d.category, d.itemLabel);
        row.delivered += d.quantity;
    }
    for (const s of sales){
        const row = rowFor(s.category, s.itemLabel);
        row.sold += s.quantity;
        row.revenue += s.totalAmount;
    }
    for (const row of rows.values()){
        row.stock = row.delivered - row.sold;
    }
    return [
        ...rows.values()
    ].sort((a, b)=>a.itemLabel.localeCompare(b.itemLabel));
}
}),
"[project]/lib/db/portal.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getPortalData",
    ()=>getPortalData
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$db$2f$workspace$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/db/workspace.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$payroll$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/payroll.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$aggregate$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/aggregate.ts [app-route] (ecmascript)");
;
;
;
async function getPortalData(scope) {
    const state = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$db$2f$workspace$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getWorkspace"])(scope.orgOwnerId);
    if (scope.role === "treasury") {
        // Org-wide, like promoter — Treasury needs to see every school's queue,
        // not just one. Requisitions are returned newest-first so the pending
        // queue naturally surfaces what's waiting on a decision.
        const clientName = new Map(state.clients.map((c)=>[
                c.id,
                c.name
            ]));
        return {
            role: scope.role,
            period: state.period,
            clients: state.clients.map((c)=>({
                    id: c.id,
                    name: c.name,
                    currency: c.currency
                })),
            requisitions: [
                ...state.requisitions
            ].sort((a, b)=>a.submittedAt < b.submittedAt ? 1 : -1).map((r)=>({
                    ...r,
                    clientName: clientName.get(r.clientId) ?? r.clientId
                })),
            // Deliveries Treasury has already pushed out — newest first, across
            // every school, so a repeat delivery to the same item is easy to spot.
            supplyDeliveries: [
                ...state.supplyDeliveries
            ].sort((a, b)=>a.createdAt < b.createdAt ? 1 : -1).map((d)=>({
                    ...d,
                    clientName: clientName.get(d.clientId) ?? d.clientId
                }))
        };
    }
    if (scope.role === "promoter") {
        const summaries = state.clients.map((client)=>{
            const staff = state.employees.filter((e)=>e.clientId === client.id && e.status !== "inactive");
            const fields = state.fields.filter((f)=>f.clientId === client.id);
            const totalSalary = staff.reduce((sum, e)=>sum + (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$payroll$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["computePayslip"])(e, fields).gross, 0);
            const students = state.students.filter((s)=>s.clientId === client.id);
            const payments = state.feePayments.filter((p)=>p.clientId === client.id);
            const expenses = state.expenses.filter((e)=>e.clientId === client.id);
            const fin = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$aggregate$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["schoolFinancials"])(students, payments, expenses, state.period);
            return {
                clientId: client.id,
                name: client.name,
                currency: client.currency,
                studentCount: fin.studentCount,
                feesCollected: fin.feesCollected,
                feesOutstanding: fin.feesOutstanding,
                totalSalary,
                expensesThisMonth: fin.expensesThisMonth,
                net: fin.feesCollected - totalSalary - fin.expensesThisMonth
            };
        });
        // The Treasury outflow trail the promoter specifically asked to see —
        // every requisition that's actually been paid, across every school.
        const clientName = new Map(state.clients.map((c)=>[
                c.id,
                c.name
            ]));
        const outflows = state.requisitions.filter((r)=>r.status === "paid").sort((a, b)=>(a.paidAt ?? "") < (b.paidAt ?? "") ? 1 : -1).map((r)=>({
                ...r,
                clientName: clientName.get(r.clientId) ?? r.clientId
            }));
        // Supplies/logistics, per the same "Bonté Service delivers, the school
        // only sells and counts" model — the promoter sees stock, sales and
        // flagged inventory gaps across every school, not just one.
        const supplyStockByClient = {};
        const supplySummaries = state.clients.map((client)=>{
            const deliveries = state.supplyDeliveries.filter((d)=>d.clientId === client.id);
            const sales = state.supplySales.filter((s)=>s.clientId === client.id);
            const stock = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$aggregate$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["supplyStock"])(deliveries, sales);
            supplyStockByClient[client.id] = stock;
            const varianceCount = state.supplyInventoryCounts.filter((c)=>c.clientId === client.id && c.variance !== 0).length;
            return {
                clientId: client.id,
                name: client.name,
                currency: client.currency,
                unitsDelivered: stock.reduce((sum, r)=>sum + r.delivered, 0),
                unitsSold: stock.reduce((sum, r)=>sum + r.sold, 0),
                unitsOnHand: stock.reduce((sum, r)=>sum + r.stock, 0),
                revenue: stock.reduce((sum, r)=>sum + r.revenue, 0),
                varianceCount
            };
        });
        const recentSupplyDeliveries = [
            ...state.supplyDeliveries
        ].sort((a, b)=>a.createdAt < b.createdAt ? 1 : -1).slice(0, 20).map((d)=>({
                ...d,
                clientName: clientName.get(d.clientId) ?? d.clientId
            }));
        const recentSupplySales = [
            ...state.supplySales
        ].sort((a, b)=>a.createdAt < b.createdAt ? 1 : -1).slice(0, 20).map((s)=>({
                ...s,
                clientName: clientName.get(s.clientId) ?? s.clientId
            }));
        const inventoryVariances = state.supplyInventoryCounts.filter((c)=>c.variance !== 0).sort((a, b)=>a.createdAt < b.createdAt ? 1 : -1).map((c)=>({
                ...c,
                clientName: clientName.get(c.clientId) ?? c.clientId
            }));
        return {
            role: scope.role,
            period: state.period,
            summaries,
            outflows,
            supplySummaries,
            supplyStockByClient,
            recentSupplyDeliveries,
            recentSupplySales,
            inventoryVariances
        };
    }
    const client = state.clients.find((c)=>c.id === scope.clientId) ?? null;
    if (!client) {
        return {
            role: scope.role,
            period: state.period,
            client: null
        };
    }
    if (scope.role === "cashier") {
        // The only school-level role that can actually touch student records —
        // enroll, edit, and change fee status on collection. Doesn't see
        // requisitions or payslips; that's Session 1 (school_admin) and
        // Session 2 (finance)'s job respectively.
        return {
            role: scope.role,
            period: state.period,
            client,
            students: state.students.filter((s)=>s.clientId === client.id),
            feePayments: state.feePayments.filter((p)=>p.clientId === client.id),
            expenses: state.expenses.filter((e)=>e.clientId === client.id)
        };
    }
    if (scope.role === "school_admin") {
        return {
            role: scope.role,
            period: state.period,
            client,
            students: state.students.filter((s)=>s.clientId === client.id),
            feePayments: state.feePayments.filter((p)=>p.clientId === client.id),
            expenses: state.expenses.filter((e)=>e.clientId === client.id),
            requisitions: state.requisitions.filter((r)=>r.clientId === client.id).sort((a, b)=>a.submittedAt < b.submittedAt ? 1 : -1),
            payslips: state.payslips.filter((p)=>p.clientId === client.id).sort((a, b)=>a.period < b.period ? 1 : -1).slice(0, 60),
            employees: state.employees.filter((e)=>e.clientId === client.id).map((e)=>({
                    id: e.id,
                    name: e.name,
                    position: e.position
                }))
        };
    }
    if (scope.role === "finance") {
        // "Responsable Financier" — verifies student fee status and generates
        // the financial report, but (per the spec) has no enrollment or
        // status-change rights, so students/feePayments are read-only here;
        // the write routes only ever accept "cashier".
        const students = state.students.filter((s)=>s.clientId === client.id);
        const feePayments = state.feePayments.filter((p)=>p.clientId === client.id);
        const expenses = state.expenses.filter((e)=>e.clientId === client.id);
        return {
            role: scope.role,
            period: state.period,
            client,
            students,
            feePayments,
            financials: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$aggregate$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["schoolFinancials"])(students, feePayments, expenses, state.period),
            payslips: state.payslips.filter((p)=>p.clientId === client.id).sort((a, b)=>a.period < b.period ? 1 : -1).slice(0, 120),
            employees: state.employees.filter((e)=>e.clientId === client.id).map((e)=>({
                    id: e.id,
                    name: e.name,
                    position: e.position
                }))
        };
    }
    if (scope.role === "intendance") {
        const deliveries = state.supplyDeliveries.filter((d)=>d.clientId === client.id);
        const sales = state.supplySales.filter((s)=>s.clientId === client.id);
        return {
            role: scope.role,
            period: state.period,
            client,
            stock: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$aggregate$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["supplyStock"])(deliveries, sales),
            deliveries: [
                ...deliveries
            ].sort((a, b)=>a.createdAt < b.createdAt ? 1 : -1),
            sales: [
                ...sales
            ].sort((a, b)=>a.createdAt < b.createdAt ? 1 : -1),
            inventoryCounts: [
                ...state.supplyInventoryCounts
            ].filter((c)=>c.clientId === client.id).sort((a, b)=>a.createdAt < b.createdAt ? 1 : -1)
        };
    }
    // teacher
    const employee = state.employees.find((e)=>e.id === scope.employeeId) ?? null;
    return {
        role: scope.role,
        period: state.period,
        client,
        employee,
        payslips: employee ? state.payslips.filter((p)=>p.employeeId === employee.id).sort((a, b)=>a.period < b.period ? 1 : -1) : []
    };
}
}),
"[project]/lib/db/users.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "activateAccount",
    ()=>activateAccount,
    "createBootstrapAdmin",
    ()=>createBootstrapAdmin,
    "createTeamMember",
    ()=>createTeamMember,
    "deleteUser",
    ()=>deleteUser,
    "findUserByEmail",
    ()=>findUserByEmail,
    "findUserById",
    ()=>findUserById,
    "findUserByInviteToken",
    ()=>findUserByInviteToken,
    "hasAnyUser",
    ()=>hasAnyUser,
    "issuePasswordResetToken",
    ()=>issuePasswordResetToken,
    "listTeamMembers",
    ()=>listTeamMembers,
    "regenerateInvite",
    ()=>regenerateInvite,
    "updateTeamMember",
    ()=>updateTeamMember
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$crypto__$5b$external$5d$__$28$crypto$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/crypto [external] (crypto, cjs)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$mongodb$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/mongodb.ts [app-route] (ecmascript)");
;
;
const INVITE_TTL_MS = 7 * 24 * 60 * 60 * 1000; // 7 days
function usersCollection() {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$mongodb$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getDb"])().then((db)=>db.collection("users"));
}
async function findUserByEmail(email) {
    const users = await usersCollection();
    return users.findOne({
        email: email.toLowerCase().trim()
    });
}
async function findUserById(id) {
    const users = await usersCollection();
    return users.findOne({
        _id: id
    });
}
async function findUserByInviteToken(token) {
    const users = await usersCollection();
    return users.findOne({
        inviteToken: token
    });
}
async function listTeamMembers(orgOwnerId) {
    const users = await usersCollection();
    return users.find({
        orgOwnerId,
        role: {
            $ne: "super_admin"
        }
    }).sort({
        createdAt: -1
    }).toArray();
}
async function hasAnyUser() {
    const users = await usersCollection();
    const count = await users.countDocuments({}, {
        limit: 1
    });
    return count > 0;
}
async function createBootstrapAdmin(params) {
    const id = `u_${Date.now().toString(36)}${Math.random().toString(36).slice(2, 8)}`;
    return insertUser({
        _id: id,
        name: params.name.trim(),
        email: params.email,
        passwordHash: params.passwordHash,
        role: "super_admin",
        orgOwnerId: id,
        clientId: null,
        employeeId: null,
        inviteToken: null,
        inviteExpires: null
    });
}
async function createTeamMember(params) {
    const inviteToken = __TURBOPACK__imported__module__$5b$externals$5d2f$crypto__$5b$external$5d$__$28$crypto$2c$__cjs$29$__["default"].randomBytes(32).toString("hex");
    const user = await insertUser({
        _id: `u_${Date.now().toString(36)}${Math.random().toString(36).slice(2, 8)}`,
        name: params.name.trim(),
        email: params.email,
        passwordHash: "",
        role: params.role,
        orgOwnerId: params.orgOwnerId,
        clientId: params.clientId,
        employeeId: params.employeeId,
        inviteToken,
        inviteExpires: new Date(Date.now() + INVITE_TTL_MS).toISOString()
    });
    return {
        user,
        inviteToken
    };
}
async function regenerateInvite(id) {
    const users = await usersCollection();
    const inviteToken = __TURBOPACK__imported__module__$5b$externals$5d2f$crypto__$5b$external$5d$__$28$crypto$2c$__cjs$29$__["default"].randomBytes(32).toString("hex");
    const result = await users.findOneAndUpdate({
        _id: id,
        passwordHash: ""
    }, {
        $set: {
            inviteToken,
            inviteExpires: new Date(Date.now() + INVITE_TTL_MS).toISOString()
        }
    });
    return result ? inviteToken : null;
}
async function issuePasswordResetToken(email) {
    const users = await usersCollection();
    const token = __TURBOPACK__imported__module__$5b$externals$5d2f$crypto__$5b$external$5d$__$28$crypto$2c$__cjs$29$__["default"].randomBytes(32).toString("hex");
    const result = await users.findOneAndUpdate({
        email: email.toLowerCase().trim()
    }, {
        $set: {
            inviteToken: token,
            inviteExpires: new Date(Date.now() + INVITE_TTL_MS).toISOString()
        }
    }, {
        returnDocument: "after"
    });
    return result ? {
        user: result,
        token
    } : null;
}
async function activateAccount(token, passwordHash) {
    const users = await usersCollection();
    const user = await users.findOne({
        inviteToken: token
    });
    if (!user) return null;
    if (!user.inviteExpires || new Date(user.inviteExpires).getTime() < Date.now()) {
        return null;
    }
    const updated = {
        ...user,
        passwordHash,
        inviteToken: null,
        inviteExpires: null
    };
    await users.updateOne({
        _id: user._id
    }, {
        $set: {
            passwordHash,
            inviteToken: null,
            inviteExpires: null
        }
    });
    return updated;
}
async function deleteUser(id) {
    const users = await usersCollection();
    await users.deleteOne({
        _id: id
    });
}
async function updateTeamMember(id, orgOwnerId, patch) {
    const users = await usersCollection();
    const update = {};
    if (patch.name !== undefined) update.name = patch.name.trim();
    if (patch.role !== undefined) update.role = patch.role;
    if (patch.clientId !== undefined) update.clientId = patch.clientId;
    if (patch.employeeId !== undefined) update.employeeId = patch.employeeId;
    return users.findOneAndUpdate({
        _id: id,
        orgOwnerId,
        role: {
            $ne: "super_admin"
        }
    }, {
        $set: update
    }, {
        returnDocument: "after"
    });
}
async function insertUser(doc) {
    const users = await usersCollection();
    const email = doc.email.toLowerCase().trim();
    const existing = await users.findOne({
        email
    });
    if (existing) {
        throw new Error("An account with that email already exists.");
    }
    const full = {
        ...doc,
        email,
        createdAt: new Date().toISOString()
    };
    await users.insertOne(full);
    return full;
}
}),
"[project]/lib/db/workspace.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "addExpenseScoped",
    ()=>addExpenseScoped,
    "addStudentScoped",
    ()=>addStudentScoped,
    "appendTeamAuditLog",
    ()=>appendTeamAuditLog,
    "decideRequisitionScoped",
    ()=>decideRequisitionScoped,
    "getWorkspace",
    ()=>getWorkspace,
    "markRequisitionPaidScoped",
    ()=>markRequisitionPaidScoped,
    "recordPaymentScoped",
    ()=>recordPaymentScoped,
    "recordSupplyDeliveryScoped",
    ()=>recordSupplyDeliveryScoped,
    "recordSupplyInventoryCountScoped",
    ()=>recordSupplyInventoryCountScoped,
    "recordSupplySaleScoped",
    ()=>recordSupplySaleScoped,
    "removeExpenseScoped",
    ()=>removeExpenseScoped,
    "removeStudentScoped",
    ()=>removeStudentScoped,
    "saveWorkspace",
    ()=>saveWorkspace,
    "submitRequisitionScoped",
    ()=>submitRequisitionScoped,
    "updateExpenseScoped",
    ()=>updateExpenseScoped,
    "updateStudentScoped",
    ()=>updateStudentScoped
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$mongodb$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/mongodb.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$seed$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/seed.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$seed$2d$history$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/seed-history.ts [app-route] (ecmascript)");
;
;
;
function workspaces() {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$mongodb$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getDb"])().then((db)=>db.collection("workspaces"));
}
async function getWorkspace(orgOwnerId) {
    const col = await workspaces();
    const existing = await col.findOne({
        _id: orgOwnerId
    });
    if (existing) return existing.state;
    const seeded = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$seed$2d$history$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["seedHistory"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$seed$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["buildInitialState"])());
    await col.insertOne({
        _id: orgOwnerId,
        state: seeded,
        updatedAt: new Date().toISOString()
    });
    return seeded;
}
async function saveWorkspace(orgOwnerId, state) {
    const col = await workspaces();
    await col.updateOne({
        _id: orgOwnerId
    }, {
        $set: {
            state,
            updatedAt: new Date().toISOString()
        }
    }, {
        upsert: true
    });
}
/** Load → transform → save in one step, for narrow scoped mutations from the portal API. */ async function mutateWorkspace(orgOwnerId, fn) {
    const current = await getWorkspace(orgOwnerId);
    const next = fn(current);
    await saveWorkspace(orgOwnerId, next);
    return next;
}
function uid(prefix) {
    return `${prefix}_${Date.now().toString(36)}${Math.random().toString(36).slice(2, 8)}`;
}
/**
 * Appends one entry to the shared audit trail (`state.logs`) — the same log
 * every super_admin dashboard action writes to, so portal-side actions by
 * school admins / finance / teachers show up right alongside them. Used by
 * every scoped mutation below, and by the team-management routes.
 */ function withLog(s, clientId, kind, message, actor, sensitive) {
    return {
        ...s,
        logs: [
            {
                id: uid("log"),
                clientId,
                at: new Date().toISOString(),
                kind,
                message,
                actor,
                sensitive
            },
            ...s.logs
        ].slice(0, 1000)
    };
}
async function appendTeamAuditLog(orgOwnerId, message, actor) {
    await mutateWorkspace(orgOwnerId, (s)=>withLog(s, s.activeClientId, "team", message, actor, true));
}
async function addStudentScoped(orgOwnerId, clientId, input, actor) {
    const student = {
        ...input,
        id: uid("st"),
        clientId
    };
    await mutateWorkspace(orgOwnerId, (s)=>withLog({
            ...s,
            students: [
                ...s.students,
                student
            ]
        }, clientId, "student", `Added student ${student.name}`, actor));
    return student;
}
async function updateStudentScoped(orgOwnerId, clientId, studentId, patch, actor) {
    await mutateWorkspace(orgOwnerId, (s)=>{
        const before = s.students.find((st)=>st.id === studentId && st.clientId === clientId);
        const next = {
            ...s,
            students: s.students.map((st)=>st.id === studentId && st.clientId === clientId ? {
                    ...st,
                    ...patch
                } : st)
        };
        if (!before) return next;
        const feeChanged = patch.monthlyFee !== undefined && patch.monthlyFee !== before.monthlyFee;
        return withLog(next, clientId, "student", feeChanged ? `Changed ${before.name}'s monthly fee from ${before.monthlyFee} to ${patch.monthlyFee}` : `Updated student ${before.name}`, actor, feeChanged);
    });
}
async function removeStudentScoped(orgOwnerId, clientId, studentId, actor) {
    await mutateWorkspace(orgOwnerId, (s)=>{
        const st = s.students.find((x)=>x.id === studentId && x.clientId === clientId);
        return withLog({
            ...s,
            students: s.students.map((x)=>x.id === studentId && x.clientId === clientId ? {
                    ...x,
                    deletedAt: new Date().toISOString()
                } : x)
        }, clientId, "student", `Removed student ${st?.name ?? ""}`, actor, true);
    });
}
async function recordPaymentScoped(orgOwnerId, clientId, studentId, period, amountPaid, status, actor, note) {
    await mutateWorkspace(orgOwnerId, (s)=>{
        const student = s.students.find((st)=>st.id === studentId && st.clientId === clientId);
        if (!student) return s;
        const existing = s.feePayments.find((p)=>p.studentId === studentId && p.period === period);
        const record = {
            id: existing?.id ?? `fp_${Date.now().toString(36)}${Math.random().toString(36).slice(2, 8)}`,
            clientId,
            studentId,
            period,
            amountDue: existing?.amountDue ?? student.monthlyFee,
            amountPaid,
            status,
            paidAt: amountPaid > 0 ? new Date().toISOString() : existing?.paidAt ?? null,
            note: note ?? existing?.note ?? ""
        };
        return withLog({
            ...s,
            feePayments: existing ? s.feePayments.map((p)=>p.id === existing.id ? record : p) : [
                ...s.feePayments,
                record
            ]
        }, clientId, "payment", `Recorded ${period} payment for ${student.name}`, actor);
    });
}
async function addExpenseScoped(orgOwnerId, clientId, input, actor) {
    const expense = {
        ...input,
        id: uid("ex"),
        clientId,
        createdAt: new Date().toISOString()
    };
    await mutateWorkspace(orgOwnerId, (s)=>withLog({
            ...s,
            expenses: [
                ...s.expenses,
                expense
            ]
        }, clientId, "expense", `Logged expense: ${expense.description}`, actor));
    return expense;
}
async function updateExpenseScoped(orgOwnerId, clientId, expenseId, patch, actor) {
    await mutateWorkspace(orgOwnerId, (s)=>{
        const before = s.expenses.find((e)=>e.id === expenseId && e.clientId === clientId);
        const next = {
            ...s,
            expenses: s.expenses.map((e)=>e.id === expenseId && e.clientId === clientId ? {
                    ...e,
                    ...patch
                } : e)
        };
        if (!before) return next;
        const amountChanged = patch.amount !== undefined && patch.amount !== before.amount;
        return withLog(next, clientId, "expense", amountChanged ? `Changed "${before.description}" amount from ${before.amount} to ${patch.amount}` : `Updated expense: ${before.description}`, actor, amountChanged);
    });
}
async function removeExpenseScoped(orgOwnerId, clientId, expenseId, actor) {
    await mutateWorkspace(orgOwnerId, (s)=>{
        const e = s.expenses.find((x)=>x.id === expenseId && x.clientId === clientId);
        return withLog({
            ...s,
            expenses: s.expenses.filter((x)=>!(x.id === expenseId && x.clientId === clientId))
        }, clientId, "expense", `Removed expense: ${e?.description ?? ""}`, actor, true);
    });
}
/* ------------------------ scoped: requisitions (Treasury) ------------------------ */ //
// The fund-request pipeline: a school submits a requisition, Treasury
// (org-wide — not scoped to any one school) approves or rejects it, and an
// approved one gets marked paid with the actual amount/method once the
// money moves. Every step is logged with the actor, since this is the
// control point the client specifically asked for to curb Treasury-side
// leakage.
const CATEGORY_LABEL = {
    fund_request: "fund request",
    payroll: "payroll funding request"
};
async function submitRequisitionScoped(orgOwnerId, clientId, input, actor) {
    const requisition = {
        id: uid("req"),
        clientId,
        category: input.category,
        description: input.description,
        amountRequested: input.amountRequested,
        period: input.period,
        status: "pending",
        submittedBy: actor.name,
        submittedAt: new Date().toISOString()
    };
    await mutateWorkspace(orgOwnerId, (s)=>withLog({
            ...s,
            requisitions: [
                ...s.requisitions,
                requisition
            ]
        }, clientId, "requisition", `Submitted a ${CATEGORY_LABEL[requisition.category]}: ${requisition.description} (${requisition.amountRequested})`, actor));
    return requisition;
}
async function decideRequisitionScoped(orgOwnerId, requisitionId, decision, note, actor) {
    await mutateWorkspace(orgOwnerId, (s)=>{
        const before = s.requisitions.find((r)=>r.id === requisitionId);
        if (!before || before.status !== "pending") return s;
        const next = {
            ...s,
            requisitions: s.requisitions.map((r)=>r.id === requisitionId ? {
                    ...r,
                    status: decision,
                    decidedBy: actor.name,
                    decidedAt: new Date().toISOString(),
                    decisionNote: note
                } : r)
        };
        return withLog(next, before.clientId, "requisition", `${decision === "approved" ? "Approved" : "Rejected"} ${before.submittedBy}'s ${CATEGORY_LABEL[before.category]}: ${before.description}${note ? ` — ${note}` : ""}`, actor, true);
    });
}
async function markRequisitionPaidScoped(orgOwnerId, requisitionId, paidAmount, paymentMethod, actor) {
    await mutateWorkspace(orgOwnerId, (s)=>{
        const before = s.requisitions.find((r)=>r.id === requisitionId);
        if (!before || before.status !== "approved") return s;
        const next = {
            ...s,
            requisitions: s.requisitions.map((r)=>r.id === requisitionId ? {
                    ...r,
                    status: "paid",
                    paidAmount,
                    paidAt: new Date().toISOString(),
                    paymentMethod
                } : r)
        };
        return withLog(next, before.clientId, "requisition", `Paid out ${paidAmount} (${paymentMethod}) for ${before.submittedBy}'s ${CATEGORY_LABEL[before.category]}: ${before.description}`, actor, true);
    });
}
/* --------------------- scoped: supplies / Intendance --------------------- */ //
// Intendance never buys — Bonté Service always assures delivery of what's
// to be sold — so a delivery can only be recorded from the Treasury side,
// pushed to one school. Intendance's own actions can only sell stock back
// out or record a physical count against it; neither one can create stock
// out of nothing, which is the point.
const CATEGORY_LABEL_SUPPLY = {
    uniform: "uniform",
    shoes: "shoes",
    sweater: "sweater",
    other: "item"
};
async function recordSupplyDeliveryScoped(orgOwnerId, clientId, input, actor) {
    const delivery = {
        id: uid("sd"),
        clientId,
        category: input.category,
        itemLabel: input.itemLabel,
        quantity: input.quantity,
        deliveredAt: input.deliveredAt,
        reference: input.reference,
        recordedBy: actor.name,
        createdAt: new Date().toISOString()
    };
    await mutateWorkspace(orgOwnerId, (s)=>withLog({
            ...s,
            supplyDeliveries: [
                ...s.supplyDeliveries,
                delivery
            ]
        }, clientId, "supply", `Delivered ${delivery.quantity}× ${delivery.itemLabel} (${CATEGORY_LABEL_SUPPLY[delivery.category]}) — ref ${delivery.reference}`, actor));
    return delivery;
}
async function recordSupplySaleScoped(orgOwnerId, clientId, input, actor) {
    const sale = {
        id: uid("ss"),
        clientId,
        category: input.category,
        itemLabel: input.itemLabel,
        quantity: input.quantity,
        unitPrice: input.unitPrice,
        totalAmount: input.quantity * input.unitPrice,
        buyerName: input.buyerName,
        soldAt: input.soldAt,
        recordedBy: actor.name,
        createdAt: new Date().toISOString()
    };
    await mutateWorkspace(orgOwnerId, (s)=>withLog({
            ...s,
            supplySales: [
                ...s.supplySales,
                sale
            ]
        }, clientId, "supply", `Sold ${sale.quantity}× ${sale.itemLabel} to ${sale.buyerName} (${sale.totalAmount})`, actor));
    return sale;
}
async function recordSupplyInventoryCountScoped(orgOwnerId, clientId, input, actor) {
    const next = await mutateWorkspace(orgOwnerId, (s)=>{
        const delivered = s.supplyDeliveries.filter((d)=>d.clientId === clientId && d.category === input.category && d.itemLabel === input.itemLabel).reduce((sum, d)=>sum + d.quantity, 0);
        const sold = s.supplySales.filter((sl)=>sl.clientId === clientId && sl.category === input.category && sl.itemLabel === input.itemLabel).reduce((sum, sl)=>sum + sl.quantity, 0);
        const expectedQty = delivered - sold;
        const count = {
            id: uid("sic"),
            clientId,
            category: input.category,
            itemLabel: input.itemLabel,
            countedQty: input.countedQty,
            expectedQty,
            variance: input.countedQty - expectedQty,
            countedAt: input.countedAt,
            countedBy: actor.name,
            note: input.note ?? "",
            createdAt: new Date().toISOString()
        };
        return withLog({
            ...s,
            supplyInventoryCounts: [
                ...s.supplyInventoryCounts,
                count
            ]
        }, clientId, "supply", count.variance === 0 ? `Counted ${count.itemLabel}: matches (${count.countedQty})` : `Counted ${count.itemLabel}: ${count.countedQty} vs expected ${count.expectedQty} (${count.variance > 0 ? "+" : ""}${count.variance})`, actor, count.variance !== 0);
    });
    // The mutation always appends exactly one new count, so it's the last entry.
    return next.supplyInventoryCounts[next.supplyInventoryCounts.length - 1];
}
}),
"[project]/lib/format.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// lib/format.ts
__turbopack_context__.s([
    "CURRENCY_SYMBOL",
    ()=>CURRENCY_SYMBOL,
    "compactMoney",
    ()=>compactMoney,
    "formatDate",
    ()=>formatDate,
    "initials",
    ()=>initials,
    "maskPhone",
    ()=>maskPhone,
    "money",
    ()=>money,
    "moneyPrecise",
    ()=>moneyPrecise,
    "payDate",
    ()=>payDate,
    "periodLabel",
    ()=>periodLabel,
    "periodShort",
    ()=>periodShort,
    "recentPeriods",
    ()=>recentPeriods,
    "shiftPeriod",
    ()=>shiftPeriod,
    "slug",
    ()=>slug,
    "timeAgo",
    ()=>timeAgo,
    "toPeriod",
    ()=>toPeriod,
    "uid",
    ()=>uid
]);
const CURRENCY_SYMBOL = {
    USD: "$",
    NGN: "\u20a6",
    GBP: "\u00a3",
    EUR: "\u20ac",
    KES: "KSh",
    ZAR: "R",
    GHS: "\u20b5"
};
function money(value, currency = "USD") {
    const sym = CURRENCY_SYMBOL[currency] ?? "$";
    const rounded = Math.round(value);
    return `${sym}${rounded.toLocaleString("en-US")}`;
}
function moneyPrecise(value, currency = "USD") {
    const sym = CURRENCY_SYMBOL[currency] ?? "$";
    return `${sym}${value.toLocaleString("en-US", {
        minimumFractionDigits: 2,
        maximumFractionDigits: 2
    })}`;
}
function compactMoney(value, currency = "USD") {
    const sym = CURRENCY_SYMBOL[currency] ?? "$";
    if (Math.abs(value) >= 1_000_000) return `${sym}${(value / 1_000_000).toFixed(1)}m`;
    if (Math.abs(value) >= 1_000) return `${sym}${Math.round(value / 1000)}k`;
    return `${sym}${Math.round(value)}`;
}
function initials(name) {
    return name.trim().split(/\s+/).slice(0, 2).map((n)=>n[0]?.toUpperCase() ?? "").join("");
}
const MONTHS = [
    "January",
    "February",
    "March",
    "April",
    "May",
    "June",
    "July",
    "August",
    "September",
    "October",
    "November",
    "December"
];
function periodLabel(period) {
    const [y, m] = period.split("-");
    const idx = Number(m) - 1;
    return `${MONTHS[idx] ?? ""} ${y}`;
}
function periodShort(period) {
    const [y, m] = period.split("-");
    const idx = Number(m) - 1;
    return `${(MONTHS[idx] ?? "").slice(0, 3)} ${y}`;
}
function toPeriod(d) {
    return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}`;
}
function shiftPeriod(period, delta) {
    const [y, m] = period.split("-").map(Number);
    const d = new Date(y, m - 1 + delta, 1);
    return toPeriod(d);
}
function recentPeriods(period, n) {
    return Array.from({
        length: n
    }, (_, i)=>shiftPeriod(period, i - (n - 1)));
}
function payDate(period, day) {
    const [y, m] = period.split("-").map(Number);
    const lastDay = new Date(y, m, 0).getDate();
    return new Date(y, m - 1, Math.min(day, lastDay));
}
function formatDate(d) {
    const date = typeof d === "string" ? new Date(d) : d;
    return date.toLocaleDateString("en-US", {
        month: "short",
        day: "numeric",
        year: "numeric"
    });
}
function timeAgo(iso) {
    const then = new Date(iso).getTime();
    const diff = Date.now() - then;
    const mins = Math.floor(diff / 60000);
    if (mins < 1) return "just now";
    if (mins < 60) return `${mins}m ago`;
    const hrs = Math.floor(mins / 60);
    if (hrs < 24) return `${hrs}h ago`;
    const days = Math.floor(hrs / 24);
    if (days === 1) return "yesterday";
    if (days < 30) return `${days}d ago`;
    return formatDate(iso);
}
let counter = 0;
function uid(prefix = "id") {
    counter += 1;
    return `${prefix}_${Date.now().toString(36)}${counter.toString(36)}${Math.floor(Math.random() * 1e6).toString(36)}`;
}
function maskPhone(phone) {
    if (!phone) return "—";
    const digits = phone.replace(/\D/g, "");
    if (digits.length < 5) return phone;
    return `${phone.slice(0, phone.length - 4).replace(/\d(?=\d{0}$)/g, "")}${phone.slice(-4)}`;
}
function slug(value) {
    return value.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
}
}),
"[project]/lib/mongodb.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getDb",
    ()=>getDb,
    "getMongoClientPromise",
    ()=>getMongoClientPromise
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$mongodb__$5b$external$5d$__$28$mongodb$2c$__cjs$2c$__$5b$project$5d2f$node_modules$2f$mongodb$29$__ = __turbopack_context__.i("[externals]/mongodb [external] (mongodb, cjs, [project]/node_modules/mongodb)");
;
const uri = process.env.MONGODB_URI;
const dbName = process.env.MONGODB_DB || "payroll_desk";
let client = null;
let clientPromise = null;
function getMongoClientPromise() {
    if (!uri) {
        throw new Error("MONGODB_URI is not set. Add it to your .env.local — see .env.example.");
    }
    if ("TURBOPACK compile-time truthy", 1) {
        if (!/*TURBOPACK member replacement*/ __turbopack_context__.g._mongoClientPromise) {
            client = new __TURBOPACK__imported__module__$5b$externals$5d2f$mongodb__$5b$external$5d$__$28$mongodb$2c$__cjs$2c$__$5b$project$5d2f$node_modules$2f$mongodb$29$__["MongoClient"](uri);
            /*TURBOPACK member replacement*/ __turbopack_context__.g._mongoClientPromise = client.connect();
        }
        return /*TURBOPACK member replacement*/ __turbopack_context__.g._mongoClientPromise;
    }
    //TURBOPACK unreachable
    ;
}
async function getDb() {
    const c = await getMongoClientPromise();
    const db = c.db(dbName);
    await ensureIndexes(db);
    return db;
}
// Guards against creating the same indexes on every request — this only
// actually hits MongoDB once per server process (createIndex is otherwise
// idempotent, but there's no reason to round-trip for it every time).
let indexesEnsured = null;
function ensureIndexes(db) {
    if (!indexesEnsured) {
        indexesEnsured = db.collection("users").createIndex({
            email: 1
        }, {
            unique: true
        }).then(()=>undefined).catch((err)=>{
            console.error("Could not ensure users.email unique index:", err);
            indexesEnsured = null; // let the next call retry
        });
    }
    return indexesEnsured;
}
}),
"[project]/lib/payroll.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// lib/payroll.ts
__turbopack_context__.s([
    "CHANNEL_LABEL",
    ()=>CHANNEL_LABEL,
    "TOKEN_LIST",
    ()=>TOKEN_LIST,
    "computePayslip",
    ()=>computePayslip,
    "fieldApplies",
    ()=>fieldApplies,
    "fieldsFor",
    ()=>fieldsFor,
    "grossFor",
    ()=>grossFor,
    "makePayslip",
    ()=>makePayslip,
    "payslipStatus",
    ()=>payslipStatus,
    "renderTemplate",
    ()=>renderTemplate
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$format$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/format.ts [app-route] (ecmascript)");
;
function fieldApplies(field, employee) {
    if (field.departmentIds.length === 0) return true;
    return field.departmentIds.includes(employee.departmentId);
}
function fieldsFor(fields, employee) {
    return fields.filter((f)=>fieldApplies(f, employee)).sort((a, b)=>a.order - b.order);
}
function computePayslip(employee, allFields) {
    const applicable = fieldsFor(allFields, employee);
    const base = employee.baseSalary;
    const earnings = [];
    const deductions = [];
    const info = [];
    for (const f of applicable){
        if (f.category === "info") {
            const override = employee.values[f.id];
            info.push({
                fieldId: f.id,
                label: f.label,
                category: "info",
                amount: 0,
                display: "",
                text: override || f.textValue || "—"
            });
            continue;
        }
        if (f.category === "earning") {
            if (f.system) {
                earnings.push({
                    fieldId: f.id,
                    label: f.label,
                    category: "earning",
                    amount: base,
                    display: "Base"
                });
                continue;
            }
            let amount = 0;
            let display = "";
            if (f.type === "fixed") {
                amount = f.amount;
                display = "Fixed";
            } else if (f.type === "percent") {
                amount = base * f.amount / 100;
                display = `${f.amount}% of base`;
            } else if (f.type === "perEmployee") {
                amount = Number(employee.values[f.id] ?? 0);
                display = "Per employee";
            }
            const override = employee.values[f.id];
            if (f.type !== "perEmployee" && override !== undefined && override !== "") {
                amount = Number(override);
                display = "Custom";
            }
            if (amount !== 0 || f.required) {
                earnings.push({
                    fieldId: f.id,
                    label: f.label,
                    category: "earning",
                    amount,
                    display
                });
            }
        }
    }
    const gross = earnings.reduce((s, l)=>s + l.amount, 0);
    for (const f of applicable){
        if (f.category !== "deduction") continue;
        let amount = 0;
        let display = "";
        if (f.type === "fixed") {
            amount = f.amount;
            display = "Fixed";
        } else if (f.type === "percent") {
            amount = gross * f.amount / 100;
            display = `${f.amount}% of gross`;
        } else if (f.type === "perEmployee") {
            amount = Number(employee.values[f.id] ?? 0);
            display = "Per employee";
        }
        const override = employee.values[f.id];
        if (f.type !== "perEmployee" && override !== undefined && override !== "") {
            amount = Number(override);
            display = "Custom";
        }
        if (amount !== 0 || f.required) {
            deductions.push({
                fieldId: f.id,
                label: f.label,
                category: "deduction",
                amount,
                display
            });
        }
    }
    const totalDeductions = deductions.reduce((s, l)=>s + l.amount, 0);
    return {
        lines: [
            ...earnings,
            ...deductions,
            ...info
        ],
        earnings,
        deductions,
        info,
        gross,
        totalDeductions,
        net: gross - totalDeductions
    };
}
function makePayslip(employee, fields, period) {
    const c = computePayslip(employee, fields);
    const delivery = {};
    for (const ch of employee.channels){
        delivery[ch] = {
            state: "pending",
            at: null
        };
    }
    return {
        id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$format$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["uid"])("ps"),
        clientId: employee.clientId,
        employeeId: employee.id,
        period,
        lines: c.lines,
        gross: c.gross,
        totalDeductions: c.totalDeductions,
        net: c.net,
        status: "draft",
        generatedAt: new Date().toISOString(),
        delivery
    };
}
function payslipStatus(p) {
    const records = Object.values(p.delivery);
    if (records.length === 0) return "draft";
    const sent = records.filter((r)=>r.state === "sent").length;
    const failed = records.filter((r)=>r.state === "failed").length;
    if (sent === records.length) return "sent";
    if (failed > 0 && sent + failed === records.length) return sent > 0 ? "partial" : "failed";
    if (sent > 0) return "partial";
    return "draft";
}
function grossFor(employee, fields) {
    return computePayslip(employee, fields).gross;
}
const CHANNEL_LABEL = {
    email: "Email",
    whatsapp: "WhatsApp"
};
const TOKEN_LIST = [
    {
        token: "{{first_name}}",
        description: "Employee first name"
    },
    {
        token: "{{full_name}}",
        description: "Employee full name"
    },
    {
        token: "{{client}}",
        description: "Client organisation name"
    },
    {
        token: "{{period}}",
        description: "Pay period, e.g. August 2026"
    },
    {
        token: "{{net_pay}}",
        description: "Net payable amount"
    },
    {
        token: "{{gross_pay}}",
        description: "Gross pay before deductions"
    },
    {
        token: "{{pay_date}}",
        description: "Date wages land"
    },
    {
        token: "{{department}}",
        description: "Employee department"
    },
    {
        token: "{{position}}",
        description: "Job title"
    }
];
function renderTemplate(tpl, ctx) {
    return tpl.replace(/\{\{(\w+)\}\}/g, (_, key)=>{
        const value = ctx[key];
        return value !== undefined ? value : `{{${key}}}`;
    });
}
}),
"[project]/lib/seed-history.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// lib/seed-history.ts
// No "use client" here on purpose — this is imported both by the client
// PayrollProvider (lib/store.tsx) and by server-only code (lib/db/workspace.ts)
// when seeding a brand-new account's workspace.
__turbopack_context__.s([
    "seedHistory",
    ()=>seedHistory
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$payroll$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/payroll.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$format$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/format.ts [app-route] (ecmascript)");
;
;
function seedHistory(state) {
    const periods = [
        1,
        2,
        3,
        4,
        5
    ].map((i)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$format$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["shiftPeriod"])(state.period, -i));
    const payslips = [];
    const logs = [];
    for (const period of periods){
        for (const emp of state.employees){
            if (emp.status === "inactive") continue;
            const fields = state.fields.filter((f)=>f.clientId === emp.clientId);
            const ps = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$payroll$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["makePayslip"])(emp, fields, period);
            const sentAt = new Date(`${period}-28T09:15:00`).toISOString();
            for (const ch of Object.keys(ps.delivery)){
                ps.delivery[ch] = {
                    state: "sent",
                    at: sentAt
                };
            }
            ps.status = "sent";
            ps.generatedAt = new Date(`${period}-27T08:00:00`).toISOString();
            payslips.push(ps);
        }
    }
    const now = Date.now();
    const activity = [
        [
            2 * 3600e3,
            "generate",
            "Generated 25 payslips for Acme Corp"
        ],
        [
            5 * 3600e3,
            "employee",
            "Added employee Ben Carter to Customer Support"
        ],
        [
            26 * 3600e3,
            "department",
            "Updated department head for Customer Support"
        ],
        [
            30 * 3600e3,
            "send",
            "Delivered 6 payslips to Engineering via email"
        ],
        [
            50 * 3600e3,
            "employee",
            "Marked Ava Thompson as on leave"
        ],
        [
            74 * 3600e3,
            "field",
            "Added Sales Commission field (Sales only)"
        ]
    ];
    for (const [ago, kind, message] of activity){
        logs.push({
            id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$format$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["uid"])("log"),
            clientId: "c_acme",
            at: new Date(now - ago).toISOString(),
            kind,
            message
        });
    }
    return {
        ...state,
        payslips,
        logs
    };
}
}),
"[project]/lib/seed.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// lib/seed.ts
__turbopack_context__.s([
    "buildInitialState",
    ()=>buildInitialState,
    "clients",
    ()=>clients,
    "templates",
    ()=>templates
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$format$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/format.ts [app-route] (ecmascript)");
;
const COLORS = [
    "pine",
    "gold",
    "olive",
    "pine-mid",
    "clay",
    "pine-deep"
];
const clients = [
    {
        id: "c_acme",
        name: "Acme Corp",
        domain: "acme.io",
        description: "Software & platform services",
        color: "pine",
        currency: "USD",
        payDay: 31,
        createdAt: "2023-02-14"
    },
    {
        id: "c_globex",
        name: "Globex Digital",
        domain: "globex.io",
        description: "Creative digital agency",
        color: "gold",
        currency: "GBP",
        payDay: 28,
        createdAt: "2024-06-03"
    },
    {
        id: "c_northwind",
        name: "Northwind Retail",
        domain: "northwind.shop",
        description: "Retail & e-commerce",
        color: "olive",
        currency: "USD",
        payDay: 25,
        createdAt: "2025-01-20"
    },
    {
        id: "c_sunrise",
        name: "Sunrise Academy",
        domain: "sunriseacademy.edu",
        description: "Primary & secondary school",
        color: "pine-mid",
        currency: "USD",
        payDay: 28,
        createdAt: "2022-09-01"
    }
];
const acmeDepts = [
    {
        key: "eng",
        name: "Engineering",
        description: "Product development, platform and infrastructure",
        staff: [
            [
                "Maya Patel",
                "CTO",
                12500
            ],
            [
                "Ethan Brooks",
                "Senior Engineer",
                9200
            ],
            [
                "Nora Haddad",
                "Senior Engineer",
                8800
            ],
            [
                "Owen Clark",
                "Engineer",
                6400
            ],
            [
                "Ava Thompson",
                "Engineer",
                6100,
                "leave"
            ],
            [
                "Leo Martins",
                "QA Engineer",
                5200
            ]
        ]
    },
    {
        key: "sales",
        name: "Sales",
        description: "Revenue, accounts and business development",
        staff: [
            [
                "Jordan Lee",
                "Head of Sales",
                8200
            ],
            [
                "Carlos Rivera",
                "Account Executive",
                7200
            ],
            [
                "Nadia Farouk",
                "Account Executive",
                6400
            ],
            [
                "Isabel Moreno",
                "Sales Development Rep",
                5600
            ]
        ]
    },
    {
        key: "mkt",
        name: "Marketing",
        description: "Brand, growth and content",
        staff: [
            [
                "Sofia Reyes",
                "Marketing Director",
                8200
            ],
            [
                "Liam Osei",
                "Growth Manager",
                6200
            ],
            [
                "Zara Blake",
                "Content Lead",
                5400
            ]
        ]
    },
    {
        key: "hr",
        name: "Human Resources",
        description: "People, culture and recruiting",
        staff: [
            [
                "Daniel Kim",
                "HR Manager",
                7600
            ],
            [
                "Grace Wanjiru",
                "People Partner",
                5600
            ]
        ]
    },
    {
        key: "fin",
        name: "Finance",
        description: "Accounting, budgeting and compliance",
        staff: [
            [
                "Amara Okafor",
                "Finance Director",
                9000
            ],
            [
                "Hana Zhou",
                "Financial Analyst",
                7000
            ],
            [
                "Luca Neri",
                "Accountant",
                6400
            ]
        ]
    },
    {
        key: "ops",
        name: "Operations",
        description: "Logistics, facilities and support",
        staff: [
            [
                "Lucas Mendes",
                "Operations Manager",
                7000
            ],
            [
                "Hugo Silva",
                "Operations Analyst",
                5600
            ],
            [
                "Omar Faruk",
                "Facilities Coordinator",
                5000
            ]
        ]
    },
    {
        key: "cs",
        name: "Customer Support",
        description: "Client success and helpdesk",
        staff: [
            [
                "Priya Nair",
                "Support Lead",
                5300
            ],
            [
                "Sena Turan",
                "Support Specialist",
                3900
            ],
            [
                "Elena Rossi",
                "Support Specialist",
                3600
            ],
            [
                "Ben Carter",
                "Support Associate",
                3300
            ]
        ]
    }
];
const globexDepts = [
    {
        key: "design",
        name: "Design Studio",
        description: "Brand identity and product design",
        staff: [
            [
                "Iris Calloway",
                "Creative Director",
                7400
            ],
            [
                "Theo Nakamura",
                "Senior Designer",
                5200
            ],
            [
                "Mila Vance",
                "Motion Designer",
                4600
            ]
        ]
    },
    {
        key: "accounts",
        name: "Client Accounts",
        description: "Account management and delivery",
        staff: [
            [
                "Rowan Blake",
                "Account Director",
                6800
            ],
            [
                "Sadie Wren",
                "Account Manager",
                4400
            ]
        ]
    },
    {
        key: "studio",
        name: "Production",
        description: "Film, photo and post-production",
        staff: [
            [
                "Kofi Mensah",
                "Producer",
                5600
            ],
            [
                "Elsa Lindqvist",
                "Editor",
                4200
            ]
        ]
    },
    {
        key: "admin",
        name: "Studio Admin",
        description: "Finance and office operations",
        staff: [
            [
                "Harriet Doyle",
                "Studio Manager",
                4000
            ]
        ]
    }
];
const northwindDepts = [
    {
        key: "store",
        name: "Store Floor",
        description: "In-store sales and merchandising",
        staff: [
            [
                "Marcus Hale",
                "Store Manager",
                5200
            ],
            [
                "Tara Boyd",
                "Shift Supervisor",
                3600
            ],
            [
                "Kai Ferreira",
                "Sales Associate",
                2900
            ]
        ]
    },
    {
        key: "warehouse",
        name: "Warehouse",
        description: "Stock, picking and dispatch",
        staff: [
            [
                "Dmitri Volkov",
                "Warehouse Lead",
                4200
            ],
            [
                "Nia Roberts",
                "Fulfilment Associate",
                3100
            ]
        ]
    },
    {
        key: "ecom",
        name: "E-commerce",
        description: "Online storefront and listings",
        staff: [
            [
                "Yara Haddad",
                "E-commerce Manager",
                5800
            ],
            [
                "Felix Adeyemi",
                "Listings Specialist",
                3400
            ]
        ]
    },
    {
        key: "care",
        name: "Customer Care",
        description: "Orders, returns and enquiries",
        staff: [
            [
                "Jonah Pike",
                "Care Specialist",
                3000
            ]
        ]
    },
    {
        key: "finance",
        name: "Finance",
        description: "Bookkeeping and payroll liaison",
        staff: [
            [
                "Beatrice Okonkwo",
                "Bookkeeper",
                4400
            ]
        ]
    }
];
const sunriseDepts = [
    {
        key: "teaching",
        name: "Teaching Staff",
        description: "Classroom teachers across all grades",
        staff: [
            [
                "Grace Mbeki",
                "Head Teacher",
                1800
            ],
            [
                "Samuel Osei",
                "Grade 5 Teacher",
                1200
            ],
            [
                "Fatou Diallo",
                "Grade 4 Teacher",
                1150
            ],
            [
                "Peter Nyong",
                "Grade 3 Teacher",
                1100
            ],
            [
                "Aisha Bello",
                "Grade 2 Teacher",
                1050
            ],
            [
                "Emmanuel Tetteh",
                "Grade 1 Teacher",
                1000
            ]
        ]
    },
    {
        key: "admin",
        name: "Administration",
        description: "School office, finance and support staff",
        staff: [
            [
                "Ruth Adjei",
                "School Administrator",
                1400
            ],
            [
                "Kwame Asante",
                "Finance Officer",
                1300
            ],
            [
                "Comfort Boateng",
                "Office Assistant",
                700
            ],
            [
                "Yaw Owusu",
                "Groundskeeper",
                600
            ]
        ]
    }
];
const DEPT_SETS = {
    c_acme: acmeDepts,
    c_globex: globexDepts,
    c_northwind: northwindDepts,
    c_sunrise: sunriseDepts
};
function emailFor(name, domain) {
    return `${name.toLowerCase().replace(/[^a-z ]/g, "").split(" ").join(".")}@${domain}`;
}
function buildOrg() {
    const departments = [];
    const employees = [];
    for (const client of clients){
        const set = DEPT_SETS[client.id];
        let n = 0;
        set.forEach((dept, di)=>{
            const deptId = `d_${client.id.slice(2)}_${dept.key}`;
            const members = [];
            dept.staff.forEach((s, si)=>{
                n += 1;
                const [name, position, baseSalary, status] = s;
                const code = `${client.name.slice(0, 2).toUpperCase()}-${String(n).padStart(3, "0")}`;
                members.push({
                    id: `e_${client.id.slice(2)}_${dept.key}_${si}`,
                    clientId: client.id,
                    departmentId: deptId,
                    code,
                    name,
                    email: emailFor(name, client.domain),
                    phone: `+1 555 0${String(100 + n).slice(-3)} ${String(1000 + n * 7).slice(-4)}`,
                    position,
                    baseSalary,
                    status: status ?? "active",
                    joinDate: `20${20 + n % 6}-${String(n * 3 % 12 + 1).padStart(2, "0")}-${String(n * 5 % 27 + 1).padStart(2, "0")}`,
                    channels: si % 4 === 3 ? [
                        "email"
                    ] : [
                        "email",
                        "whatsapp"
                    ],
                    values: {}
                });
            });
            departments.push({
                id: deptId,
                clientId: client.id,
                name: dept.name,
                description: dept.description,
                headId: members[0]?.id ?? null,
                color: COLORS[di % COLORS.length]
            });
            employees.push(...members);
        });
    }
    return {
        departments,
        employees
    };
}
/* ------------------------------------------------------------------ */ /* Payslip field definitions                                           */ /* ------------------------------------------------------------------ */ function baseField(clientId) {
    return {
        id: `f_${clientId.slice(2)}_basic`,
        clientId,
        label: "Basic Salary",
        category: "earning",
        type: "perEmployee",
        amount: 0,
        textValue: "",
        required: true,
        departmentIds: [],
        order: 0,
        note: "Pulled from each employee record",
        system: true
    };
}
const acmeFields = [
    {
        label: "Housing Allowance",
        category: "earning",
        type: "fixed",
        amount: 800,
        textValue: "",
        required: false,
        departmentIds: [],
        note: "Flat monthly housing support"
    },
    {
        label: "Transport Allowance",
        category: "earning",
        type: "fixed",
        amount: 300,
        textValue: "",
        required: false,
        departmentIds: [],
        note: ""
    },
    {
        label: "Performance Bonus",
        category: "earning",
        type: "percent",
        amount: 5,
        textValue: "",
        required: false,
        departmentIds: [],
        note: ""
    },
    {
        label: "Sales Commission",
        category: "earning",
        type: "percent",
        amount: 8,
        textValue: "",
        required: false,
        departmentIds: [
            "d_acme_sales"
        ],
        note: "Sales team only"
    },
    {
        label: "On-call Allowance",
        category: "earning",
        type: "fixed",
        amount: 250,
        textValue: "",
        required: false,
        departmentIds: [
            "d_acme_eng"
        ],
        note: "Engineering rota"
    },
    {
        label: "Income Tax",
        category: "deduction",
        type: "percent",
        amount: 10,
        textValue: "",
        required: true,
        departmentIds: [],
        note: ""
    },
    {
        label: "Pension Contribution",
        category: "deduction",
        type: "percent",
        amount: 5,
        textValue: "",
        required: true,
        departmentIds: [],
        note: ""
    },
    {
        label: "Health Insurance",
        category: "deduction",
        type: "fixed",
        amount: 120,
        textValue: "",
        required: false,
        departmentIds: [],
        note: ""
    },
    {
        label: "Bank Account",
        category: "info",
        type: "text",
        amount: 0,
        textValue: "•••• 0000",
        required: true,
        departmentIds: [],
        note: ""
    },
    {
        label: "Tax ID",
        category: "info",
        type: "text",
        amount: 0,
        textValue: "TIN-000000",
        required: false,
        departmentIds: [],
        note: ""
    }
];
const globexFields = [
    {
        label: "Studio Allowance",
        category: "earning",
        type: "fixed",
        amount: 450,
        textValue: "",
        required: false,
        departmentIds: [],
        note: ""
    },
    {
        label: "Project Bonus",
        category: "earning",
        type: "percent",
        amount: 6,
        textValue: "",
        required: false,
        departmentIds: [],
        note: ""
    },
    {
        label: "PAYE",
        category: "deduction",
        type: "percent",
        amount: 20,
        textValue: "",
        required: true,
        departmentIds: [],
        note: ""
    },
    {
        label: "National Insurance",
        category: "deduction",
        type: "percent",
        amount: 8,
        textValue: "",
        required: true,
        departmentIds: [],
        note: ""
    },
    {
        label: "Bank Account",
        category: "info",
        type: "text",
        amount: 0,
        textValue: "•••• 0000",
        required: true,
        departmentIds: [],
        note: ""
    }
];
const northwindFields = [
    {
        label: "Shift Allowance",
        category: "earning",
        type: "fixed",
        amount: 200,
        textValue: "",
        required: false,
        departmentIds: [],
        note: ""
    },
    {
        label: "Overtime",
        category: "earning",
        type: "perEmployee",
        amount: 0,
        textValue: "",
        required: false,
        departmentIds: [],
        note: "Entered per employee each cycle"
    },
    {
        label: "Store Bonus",
        category: "earning",
        type: "percent",
        amount: 4,
        textValue: "",
        required: false,
        departmentIds: [
            "d_northwind_store"
        ],
        note: "Store floor only"
    },
    {
        label: "Income Tax",
        category: "deduction",
        type: "percent",
        amount: 12,
        textValue: "",
        required: true,
        departmentIds: [],
        note: ""
    },
    {
        label: "Uniform Levy",
        category: "deduction",
        type: "fixed",
        amount: 40,
        textValue: "",
        required: false,
        departmentIds: [
            "d_northwind_store",
            "d_northwind_warehouse"
        ],
        note: ""
    },
    {
        label: "Bank Account",
        category: "info",
        type: "text",
        amount: 0,
        textValue: "•••• 0000",
        required: true,
        departmentIds: [],
        note: ""
    }
];
const sunriseFields = [
    {
        label: "Housing Allowance",
        category: "earning",
        type: "fixed",
        amount: 150,
        textValue: "",
        required: false,
        departmentIds: [],
        note: ""
    },
    {
        label: "Transport Allowance",
        category: "earning",
        type: "fixed",
        amount: 80,
        textValue: "",
        required: false,
        departmentIds: [],
        note: ""
    },
    {
        label: "Income Tax",
        category: "deduction",
        type: "percent",
        amount: 8,
        textValue: "",
        required: true,
        departmentIds: [],
        note: ""
    },
    {
        label: "Social Security",
        category: "deduction",
        type: "percent",
        amount: 5,
        textValue: "",
        required: true,
        departmentIds: [],
        note: ""
    },
    {
        label: "Bank Account",
        category: "info",
        type: "text",
        amount: 0,
        textValue: "•••• 0000",
        required: true,
        departmentIds: [],
        note: ""
    }
];
const FIELD_SETS = {
    c_acme: acmeFields,
    c_globex: globexFields,
    c_northwind: northwindFields,
    c_sunrise: sunriseFields
};
function buildFields() {
    const out = [];
    for (const client of clients){
        out.push(baseField(client.id));
        FIELD_SETS[client.id].forEach((f, i)=>{
            out.push({
                ...f,
                id: `f_${client.id.slice(2)}_${i}`,
                clientId: client.id,
                order: i + 1
            });
        });
    }
    return out;
}
const sunriseStudents = [
    [
        "Kwabena Mensah",
        "Grade 5",
        120,
        "paid",
        120
    ],
    [
        "Abena Owusu",
        "Grade 5",
        120,
        "paid",
        120
    ],
    [
        "Yaw Boateng",
        "Grade 5",
        120,
        "unpaid",
        0
    ],
    [
        "Akosua Frimpong",
        "Grade 4",
        110,
        "paid",
        110
    ],
    [
        "Kojo Antwi",
        "Grade 4",
        110,
        "partial",
        60
    ],
    [
        "Ama Sarpong",
        "Grade 4",
        110,
        "social_case",
        0
    ],
    [
        "Kwesi Danso",
        "Grade 3",
        100,
        "paid",
        100
    ],
    [
        "Efua Asare",
        "Grade 3",
        100,
        "unpaid",
        0
    ],
    [
        "Kofi Amankwah",
        "Grade 2",
        95,
        "paid",
        95
    ],
    [
        "Adjoa Nyarko",
        "Grade 2",
        95,
        "partial",
        40
    ],
    [
        "Kwame Otieno",
        "Grade 1",
        90,
        "paid",
        90
    ],
    [
        "Abla Quaye",
        "Grade 1",
        90,
        "unpaid",
        0
    ]
];
function buildStudents() {
    return sunriseStudents.map((s, i)=>{
        const [name, className, monthlyFee] = s;
        return {
            id: `st_sunrise_${i}`,
            clientId: "c_sunrise",
            name,
            cycle: "primaire",
            className,
            guardianContact: `+233 24 ${String(100 + i).padStart(3, "0")} ${String(1000 + i * 11).slice(-4)}`,
            monthlyFee,
            status: "active",
            joinDate: `2022-09-0${i % 9 + 1}`,
            note: ""
        };
    });
}
/**
 * Builds four months of fee-payment history per student: the three months
 * before `currentPeriod` are seeded as paid in full (a clean track record),
 * and `currentPeriod` itself uses each student's seeded current status —
 * this is what makes month-to-month collection trends visible right away.
 */ function buildFeePayments(currentPeriod) {
    const out = [];
    sunriseStudents.forEach((s, i)=>{
        const [, , monthlyFee, currentStatus, currentAmountPaid] = s;
        const studentId = `st_sunrise_${i}`;
        for(let back = 3; back >= 1; back--){
            const period = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$format$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["shiftPeriod"])(currentPeriod, -back);
            out.push({
                id: `fp_sunrise_${i}_${period}`,
                clientId: "c_sunrise",
                studentId,
                period,
                amountDue: monthlyFee,
                amountPaid: monthlyFee,
                status: "paid",
                paidAt: new Date(`${period}-27T10:00:00`).toISOString(),
                note: ""
            });
        }
        out.push({
            id: `fp_sunrise_${i}_${currentPeriod}`,
            clientId: "c_sunrise",
            studentId,
            period: currentPeriod,
            amountDue: monthlyFee,
            amountPaid: currentAmountPaid,
            status: currentStatus,
            paidAt: currentAmountPaid > 0 ? new Date(Date.now() - i * 86_400_000).toISOString() : null,
            note: ""
        });
    });
    return out;
}
const sunriseExpenses = [
    [
        "fuel",
        "Generator fuel — monthly top-up",
        85,
        4
    ],
    [
        "renovation",
        "Repair of Grade 3 classroom roof",
        620,
        12
    ],
    [
        "supplies",
        "Exercise books and chalk restock",
        140,
        6
    ],
    [
        "utilities",
        "Electricity bill — August",
        210,
        9
    ],
    [
        "credit",
        "Supplier credit repayment — furniture",
        300,
        20
    ],
    [
        "maintenance",
        "Plumbing repair — staff restroom",
        95,
        15
    ]
];
function buildExpenses() {
    return sunriseExpenses.map((e, i)=>{
        const [category, description, amount, daysAgo] = e;
        const date = new Date(Date.now() - daysAgo * 86_400_000);
        return {
            id: `ex_sunrise_${i}`,
            clientId: "c_sunrise",
            category,
            description,
            amount,
            date: date.toISOString().slice(0, 10),
            submittedBy: "Ruth Adjei",
            createdAt: date.toISOString()
        };
    });
}
const sunriseDeliveries = [
    [
        "uniform",
        "Uniform — Size S",
        30,
        45,
        "BS-2024-014"
    ],
    [
        "uniform",
        "Uniform — Size M",
        40,
        45,
        "BS-2024-014"
    ],
    [
        "shoes",
        "Shoes — Size 36",
        20,
        30,
        "BS-2024-021"
    ],
    [
        "sweater",
        "Sweater — Size M",
        25,
        30,
        "BS-2024-021"
    ]
];
const sunriseSales = [
    [
        "uniform",
        "Uniform — Size S",
        5,
        25,
        "Guardian of Kwabena Mensah",
        40
    ],
    [
        "uniform",
        "Uniform — Size S",
        7,
        25,
        "Guardian of Abena Owusu",
        22
    ],
    [
        "uniform",
        "Uniform — Size M",
        10,
        25,
        "Guardian of Akosua Frimpong",
        38
    ],
    [
        "uniform",
        "Uniform — Size M",
        8,
        25,
        "Guardian of Kojo Antwi",
        15
    ],
    [
        "shoes",
        "Shoes — Size 36",
        9,
        30,
        "Guardian of Kwesi Danso",
        20
    ],
    [
        "sweater",
        "Sweater — Size M",
        10,
        15,
        "Guardian of Efua Asare",
        18
    ]
];
const sunriseCounts = [
    [
        "uniform",
        "Uniform — Size S",
        18,
        5
    ],
    [
        "uniform",
        "Uniform — Size M",
        22,
        5
    ],
    [
        "shoes",
        "Shoes — Size 36",
        8,
        5
    ],
    [
        "sweater",
        "Sweater — Size M",
        15,
        5
    ]
];
function daysAgoIso(daysAgo) {
    return new Date(Date.now() - daysAgo * 86_400_000).toISOString();
}
function buildSupplyDeliveries() {
    return sunriseDeliveries.map(([category, itemLabel, quantity, daysAgo, reference], i)=>({
            id: `sd_sunrise_${i}`,
            clientId: "c_sunrise",
            category,
            itemLabel,
            quantity,
            deliveredAt: daysAgoIso(daysAgo).slice(0, 10),
            reference,
            recordedBy: "Bonté Service",
            createdAt: daysAgoIso(daysAgo)
        }));
}
function buildSupplySales() {
    return sunriseSales.map(([category, itemLabel, quantity, unitPrice, buyerName, daysAgo], i)=>({
            id: `ss_sunrise_${i}`,
            clientId: "c_sunrise",
            category,
            itemLabel,
            quantity,
            unitPrice,
            totalAmount: quantity * unitPrice,
            buyerName,
            soldAt: daysAgoIso(daysAgo).slice(0, 10),
            recordedBy: "Ruth Adjei",
            createdAt: daysAgoIso(daysAgo)
        }));
}
function buildSupplyInventoryCounts() {
    return sunriseCounts.map(([category, itemLabel, countedQty, daysAgo], i)=>{
        const delivered = sunriseDeliveries.filter((d)=>d[0] === category && d[1] === itemLabel).reduce((sum, d)=>sum + d[2], 0);
        const sold = sunriseSales.filter((s)=>s[0] === category && s[1] === itemLabel).reduce((sum, s)=>sum + s[2], 0);
        const expectedQty = delivered - sold;
        return {
            id: `sic_sunrise_${i}`,
            clientId: "c_sunrise",
            category,
            itemLabel,
            countedQty,
            expectedQty,
            variance: countedQty - expectedQty,
            countedAt: daysAgoIso(daysAgo).slice(0, 10),
            countedBy: "Ruth Adjei",
            note: "",
            createdAt: daysAgoIso(daysAgo)
        };
    });
}
const templates = {
    emailSubject: "Your {{period}} payslip from {{client}}",
    emailBody: `Hi {{first_name}},

Your payslip for {{period}} is ready. Your net pay of {{net_pay}} will reach your account on {{pay_date}}.

A full breakdown is attached as a PDF. If anything looks off, reply to this email and we'll sort it out.

— Payroll, {{client}}`,
    whatsappBody: `*{{client}} Payroll*

Hi {{first_name}}, your {{period}} payslip is ready.

Net pay: *{{net_pay}}*
Pay date: {{pay_date}}

Tap below to view your full breakdown.`
};
function buildInitialState() {
    const { departments, employees } = buildOrg();
    const now = new Date();
    const period = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$format$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["toPeriod"])(now);
    return {
        clients,
        activeClientId: "c_acme",
        departments,
        employees,
        fields: buildFields(),
        payslips: [],
        students: buildStudents(),
        feePayments: buildFeePayments(period),
        expenses: buildExpenses(),
        requisitions: [],
        supplyDeliveries: buildSupplyDeliveries(),
        supplySales: buildSupplySales(),
        supplyInventoryCounts: buildSupplyInventoryCounts(),
        logs: [],
        templates,
        period
    };
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__09m0i72._.js.map